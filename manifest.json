{
    "manifest_version": 2,

    "name": "Mixpanel CE Visual Editor",
    "description": "This extension will inject mixpanel into every page and send data to project id 1005665",
    "version": "1.0",

    "content_scripts": [{
        "run_at": "document_start",
        "matches": ["<all_urls>"],
        "js": ["inject-mixpanel.js"]
    }],
    "permissions": [
       "tabs"
    ]
}
